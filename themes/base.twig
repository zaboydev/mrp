<?php
defined('BASEPATH') OR exit('No direct script access allowed');

require_once APPPATH .'third_party/phpti/ti.php';

define('LINK_PROTOCOL', USE_SSL ? 'https' : NULL);
define('PAGE_ROLE', $this->uri->segment(2, 'index'));

if (isset($page['title']))
  define('PAGE_TITLE', strtoupper($page['title']));
else
  if ($module['name'] === 'dashboard')
    define('PAGE_TITLE', 'DASHBOARD');
  else
    define('PAGE_TITLE', strtoupper(ucfirst( PAGE_ROLE ) .' '. $module['label']));

$this->load->helper('html');

echo html_open();
echo html_title(PAGE_TITLE);
echo html_charset();
echo meta('X-UA-Compatible', 'IE=edge', 'equiv');
echo meta('viewport', 'width=device-width, initial-scale=1.0');

echo link_tag('themes/admin_lte/assets/images/favicon-plane.ico', 'shortcut icon', 'image/x-icon');
echo link_tag('themes/admin_lte/assets/images/favicon-plane.ico', 'icon', 'image/x-icon');

emptyblock('styles');
emptyblock('body');
emptyblock('scripts');

echo html_close();
?>
