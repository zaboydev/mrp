# Inventory Item
# Inventory Item hanya dapat diimport, lainnya hanya dapat dilakukan dengan dokumen terkait (GRN, MS, dll) @FIXED 
# Inventory Item hanya menampilkan item dengan kondisi S/S @FIXED
# Inventory Item hanya menampilkan item mengacu pada user yang login, kecuali untuk main base @FIXED

# GRN FIXED
# GRN mengijinkan user untuk membuat stores baru jika belum ada @FIXED
# GRN hanya untuk main base (WISNU)
# GRN pada out base tidak dilakukan

# Material Slip
# Material Slip mengacu pada user yang login, kecuali untuk main base
# Material Slip item issued mengacu ke GRN (FIFO)
# 
